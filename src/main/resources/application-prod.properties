# Database - reads from environment variable on Koyeb (with secure fallback)
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://Akmuchhala:714ujKi5zTOXyAq0@cluster0.fps9an5.mongodb.net/marketplace?retryWrites=true&w=majority}

# Server
server.port=${PORT:8080}

# Disable static file serving (backend API only)
spring.web.resources.add-mappings=false

# Connection pooling (Atlas M0 limit is 500)
spring.data.mongodb.max-connections-per-host=10

# HTTP Compression
server.compression.enabled=true
server.compression.mime-types=application/json
server.compression.min-response-size=2048

# Logging (reduce I/O on free tier)
logging.level.root=WARN
logging.level.com.smartcrops.marketplace=INFO

# Spring profile
spring.application.name=marketplace
